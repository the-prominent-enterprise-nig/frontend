# Scenario Checklist — Implementation Status

One-page status for all module scenarios (14 from the original source, plus 15-21 added 2026-07-31 from a second client scenario map — see [module-scenarios.md](./module-scenarios.md)'s "Draft 2 additions" for the full routing table). Each row is re-verified against the actual `development` branch code in both repos (not just the plan docs' own checkmarks, which can drift) — see the full gap analysis and Implementation Log in each scenario's own plan doc for file/line evidence.

Last verified: 2026-08-07 (targeted re-audit against `origin/development` HEAD in both repos, triggered by a large volume of merges in the 2 days since the 2026-08-05 full audit — file:line evidence via `git show origin/development:<path>` / `git grep ... origin/development`, not local working trees. Both local checkouts are on unrelated branches with uncommitted WIP this pass — backend on `development` clean, frontend on `fix/financing-term-edit-delete` with 6 modified files; neither touched, see Notes. Headline change: **Scenario 10's backend merged (#89) and the 2026-08-05 live-404 breakage is resolved** — voucher approval, 3-way match, cheque printing, supplier debit memos, AP payment methods, and PO document download are all now live and correctly wired end-to-end; only auto-1%-WHT and voucher attachments remain open. Also clarified: **Scenario 21's RBAC blocker is more precisely characterized, not newly changed** — `RolesController` already has `@RequirePermissions` decorators on every route, but no `PermissionsGuard` is ever applied (not per-route, not globally) to actually enforce them, so it's still fully unguarded in practice — this looks fixed at a glance but isn't; `POST /users` remains completely unguarded and still accepts a caller-supplied `roleIds` array. Scenarios 02, 05, 08 re-confirmed unchanged (no drift from unrelated merges). Scenario 17's unmerged branch has grown substantially (full Credit Application/Promissory Note module, both repos) but is still not merged to `development`. See Notes for full detail on all of the above, plus a new unmerged `feat/inventory-transfer-partial-receipt` branch (partial/reopenable stock transfer receiving) that isn't tied to any tracked scenario gap.

Previously verified 2026-08-05 (full re-audit of all 21 rows against `development` in both repos via isolated worktrees — see Notes for that pass's findings: Scenario 15 confirmed merged and closed, Scenario 19 confirmed fully implemented and closed, Scenario 20's plan-doc RBAC warning confirmed resolved; Scenario 19 implemented 2026-08-04, then closed 2026-08-05; Scenario 16 implemented and closed 2026-08-04; Scenario 14 closed 2026-08-03; Scenario 20 fully closed 2026-08-03 — NAMIDRe + DAM; Scenario 02 re-verified 2026-08-01; Scenario 05 re-verified 2026-07-31).

- [x] **01 — POS Installment Sale** — fully closed. [plan](./scenario-01-pos-installment-sale-plan.md)
- [ ] **02 — CRM Customer Profile** — core done (2026-08-01: co-maker, duplicate detection, BM/AR merge-resolution, ID/consent capture, Lead-conversion loyalty bug); segment campaigns remain out of scope pending their own integration/scoping pass. [plan](./scenario-02-crm-customer-profile-plan.md) / [updates](./scenario-02-crm-customer-profile-updates.md)
  - [ ] Segment → campaign send mechanism doesn't exist
- [x] **03 — Reservation / Advance Sale** — fully closed. [plan](./scenario-03-reservation-advance-sale-plan.md)
- [x] **04 — POS Cross-Branch Serial** — fully closed. [plan](./scenario-04-pos-cross-branch-serial-plan.md)
- [ ] **05 — Receiving** — core gaps + both updates-doc items fixed, 2 narrow follow-ups open. [plan](./scenario-05-receiving-plan.md) / [updates](./scenario-05-receiving-updates.md)
  - [ ] Cost-view restriction only covers the Receiving flow — Item Master, Costing/Valuation, Bundles, and PO screens still show cost to everyone (14 files, deferred; likely belongs under Scenario 16 instead)
  - [ ] Freebies flag only exists in the standalone Receiving form — "Receive against PO" has no way to add an extra (unordered) freebie line
- [x] **06 — Stock Request & Inter-Branch Transfer** — fully closed (doc is stale, needs a rewrite). [plan](./scenario-06-stock-request-transfer-plan.md)
- [x] **07 — Repair Transfer** — fully closed, all 5 parts. [plan](./scenario-07-repair-transfer-plan.md)
- [ ] **08 — Caravan** — done, 2 small items open. [plan](./scenario-08-caravan-plan.md)
  - [ ] No "Consign to Branch" UI button — consigning is API-only today
  - [ ] No event name/date field
- [ ] **09 — Aircool (aircon sale + install)** — Closing Gaps 1-5 fully closed; Closing Gap 6 (type of service at job creation, from NIG's real service catalog) added 2026-08-08, not started. [plan](./scenario-09-aircool-plan.md)
  - [ ] No "type of service" concept on `ServiceDraft` — job creation only collects Branch/Title/Customer/Notes, no structured category/sub-type from NIG's 6-category service catalog, no quoted-amount capture, no materials auto-suggestion for the electrical-part sub-types
- [ ] **10 — Purchasing & Accounts Payable** — 2026-08-05's live-404 breakage is resolved: backend (`feat/scenario-10-purchasing-ap`, #89, `6fd7ffd`) merged 2026-08-05/06 and the frontend AP UI now wires up correctly end-to-end (verified route-for-route, DTO-for-DTO). PR→PO→RR, voucher approval, 3-way match, cheque printing, supplier debit memos, AP payment methods (incl. GL mapping), PO document download, PO freebies (Part 8), and PO-line supplier discounts are all live and functioning. Two narrow gaps remain. [plan](./scenario-10-purchasing-ap-plan.md) / [updates](./scenario-10-purchasing-ap-updates.md)
  - [ ] Auto 1% withholding on payment — `RecordAPPaymentDto.withholdingAmount` is still a plain optional number the caller supplies, no automatic 1% computation anywhere in `ap-bills.service.ts`; defaults to 0 if omitted
  - [ ] Voucher attachments — `CreateVoucherDto` (`ap-bills.dto.ts:200`) has only `voucherNumber`; no file/attachment field, so the "manual #, attachments, approval workflow" gap is only 2/3 closed
- [x] **11 — Collections & AR Aging** — doc was very stale; a real Collector/InstallmentAccount module already shipped. [plan](./scenario-11-collections-ar-aging-plan.md)
  - [ ] (minor) Generic AR aging report (`reports.service.ts`) still flat/single-clock, not integrated with the new collections aging
  - [ ] No two-step collection-receipt workflow — `recordPayment()` is still a single direct action, no OR-stub/remittance-report step (original plan doc's Closing Gap #4; not previously tracked in this checklist, found 2026-08-05)
- [x] **12 — EOD Cash & Cash-in-Transit Monitor** — fully closed, incl. company-wide monitor + Excel export. [plan](./scenario-12-eod-cit-monitor-plan.md) / [updates](./scenario-12-eod-cit-monitor-updates.md)
- [ ] **13 — Credit & Debit Memos** — supplier side now built (2026-08-07, riding Scenario 10's backend merge); customer-side `CreditMemo`/`DebitMemo` still half built. [plan](./scenario-13-credit-debit-memos-plan.md)
  - [ ] `CreditMemo` is thin: no type field, no line items, no serial link, not connected to POS returns
  - [ ] `DebitMemo` — zero implementation
  - [x] ~~Supplier-side returns/credit~~ — resolved 2026-08-07: `SupplierDebitMemo` now a real backed entity (`supplier-debit-memos.controller/service.ts`, GL posting via `journalEntryId`) and `SupplierDebitMemoDialog.tsx` calls it correctly — same fix as Scenario 10's backend merge (#89), no longer a 404
- [x] **14 — Accounting Daily & Month-End** — fully closed (2026-08-03: tax-rate approver-gating, per-branch P&L, internal-vs-net P&L view, cost-center report). [plan](./scenario-14-accounting-month-end-plan.md)
- [x] **15 — Price List Management & Approval** — fully closed (confirmed merged 2026-08-05): branch scoping, approval workflow (`pending_approval` → `active`/`rejected`/resubmit), floor-price validation, and true versioning via `supersedesId`, both frontend PR #103 (`7a58bf9`) and backend PR #99 (`044ab1b`) confirmed as ancestors of `development` HEAD. [plan](./scenario-15-price-list-management-plan.md)
- [x] **16 — Item Master Governance** — fully closed (2026-08-04): draft→submit→confirm-accounting→approve workflow, Master Data Approver role, pg_trgm near-duplicate warning. [plan](./scenario-16-item-master-governance-plan.md)
- [ ] **17 — Credit Application, Investigation & Promissory Note** — not started on `development` (re-confirmed 2026-08-07); depends on Scenario 02's co-maker entity. Substantial work exists on unmerged branch `feat/scenario-17-credit-application-promissory-note` in both repos (schema, API, Credit Investigator role/permissions, full frontend module + POS installment gate + promissory note UI, even a doc closing the scenario out) — genuinely not landed yet, not untouched work. [plan](./scenario-17-credit-application-promissory-note-plan.md)
  - [ ] No `CreditApplication` entity, no Credit Investigator role/CI record, no `PromissoryNote` on `development`
- [ ] **18 — Customer Returns, Exchanges & Disposition** — not started on `development` (re-confirmed 2026-08-05); Part 1 (Quarantine + inspection step) is built and ready on unmerged branch `feat/scenario-18-returns-exchanges-disposition` (`b91c9df`), just not landed yet. [plan](./scenario-18-returns-exchanges-disposition-plan.md)
  - [ ] No Quarantine hold, no tiered custodian+approver flow, Exchange is a vestigial enum value, not connected to `CreditMemo`
- [x] **19 — Stock Count & Inventory Adjustment Approval** — fully implemented, confirmed closed 2026-08-05: server-side count snapshot, `StockAdjustment` approval chain (submitted → confirmed → investigating → approved/rejected, deferred stock/GL posting until approve()), before/after audit values on `StockLedger`, and non-saleable status (existing `BatchStatus`/`SerialNumberStatus`) surfaced on adjustment lines. [plan](./scenario-19-stock-count-adjustment-approval-plan.md)
  - [ ] Serial-level count reconciliation — explicitly deferred (developer decision); counting stays SKU/batch-level only
- [x] **20 — Collections Reminder Track (NAMIDRe) & Delinquency Escalation (DAM)** — fully closed 2026-08-03: NAMIDRe reminder track (linked to InstallmentAccount/Collector, duplicate-suppression, auto-close-on-payment) + DAM (trigger rule + manual escalation, structured PTP tracking, legal-escalation checklist/status). [plan](./scenario-20-collections-namidre-dam-plan.md)
  - [ ] Deliberately not built (confirmed with the developer as out of scope for this pass, not a gap): a formal BM → Area Supervisor → HO AR multi-stage review/approval chain for legal escalation. Today it's a single status field anyone with the permission can move through any order; enforcing the PDF's actual stage-by-stage review workflow would be its own scoping conversation.
- [ ] **21 — Role-Based Action Queues, Maker-Checker & Approval Limits** — new (2026-07-31), not started. [plan](./scenario-21-role-queues-maker-checker-plan.md)
  - [ ] `PendingApprovalsWidget` is hardcoded mock data, no maker≠checker enforcement, `AccountingAuditLog` unused, no approval limits
  - [ ] Offline sync explicitly deferred — flagged as future work, not scoped into this doc's implementation
  - [ ] Blocking prerequisite risk — still live, more precisely characterized this pass (re-confirmed 2026-08-07): `RolesController` has `@RequirePermissions(...)` decorators on every route (a pre-existing state, not a recent change — looks like a fix at a glance), but `PermissionsGuard` is never actually applied to that controller (no per-route `@UseGuards(PermissionsGuard)`, no class-level guard beyond `JwtAuthGuard`, and no global `APP_GUARD`/`useGlobalGuards()` registration anywhere in `app.module.ts`/`main.ts`) — the decorators are inert metadata, so every roles endpoint is still fully unguarded in practice. `POST /users` remains completely unguarded (only class-level `JwtAuthGuard`) and still takes a caller-supplied, required `roleIds: string[]` directly on `CreateUserDto` — any authenticated user can still self-escalate to Business Owner. Any maker≠checker or approval-limit design built on the current permission system inherits this hole until it's patched.
- [ ] **22 — Fixed Role Hierarchy, Module Access & Protected Routes** — new (2026-08-07), developer-defined counter-check scenario (not sourced from either client PDF), not started. [plan](./scenario-22-fixed-roles-protected-routes-plan.md)
  - [ ] Shares Scenario 21's blocking prerequisite: the two live self-escalation bugs (`RolesController` unguarded, `POST /users` unguarded)
  - [ ] No concept of "founding/primary Business Owner" anywhere — a second Business Owner can today deactivate or de-role the very first Business Owner, with zero check (`users.service.ts` `remove()`/`removeRole()`/`setActiveStatus()`)
  - [ ] Protected-route enforcement (`/403`) is opt-in per page/controller, not systemic — proven pattern exists (`settings/roles`, `accounting/vendors`) but not applied everywhere
  - [ ] Two overlapping, undocumented "inventory" roles seeded (`Stock Controller` vs `Inventory`) — needs a business decision, not a silent pick
- [ ] **23 — POS Transaction ↔ Invoice Lookup Consistency** — new (2026-08-08), developer-defined counter-check scenario from a POS team meeting comment (not sourced from either client PDF), not started. [plan](./scenario-23-transaction-invoice-lookup-plan.md)
  - [ ] Charge-mode invoice generation already correctly aggregates by mode (one invoice per transaction); installment does not — `createAndPostInstallmentPlan()` runs per line, not per group of same-term lines. Enforced at the schema level (`InstallmentSchedule.posTransactionLineId` and `InstallmentAccount.installmentScheduleId` both `@unique`) — buying 2 items on the _same_ term in one visit creates 2 independent schedules/accounts/due dates today, when it should be 1. Per-line term selection itself is a confirmed intentional feature (developer-confirmed 2026-08-08), not a bug — the fix groups by shared term, it doesn't remove per-item term choice
  - [ ] Customer360's Installment Plans section (the literal "under each customer" view from the comment) fetches each line's `invoiceNumber` but never renders it — only the shared transaction number is shown. Also missing (folded in from a separate Collections comment batch, 2026-08-08): product name, brand, and rebate amount (fixed 7.5% of MI) per line — not even fetched by `getInstallmentSchedules()`, let alone rendered
  - [ ] POS transaction detail (`findOne()`) includes no invoice data at all, and the POS Transactions screen and AR Invoices search have no real transaction-number↔invoice-number cross-lookup
- [ ] **25 — Per-Invoice Detail View & Whole-Invoice Collections** — new (2026-08-08), developer-defined counter-check scenario from a CRM/Collections team meeting comment (not sourced from either client PDF), not started. [plan](./scenario-25-ar-invoice-detail-view-plan.md)
  - [ ] No per-invoice detail view exists anywhere — AR Invoices is one flat table, no drill-down page; Customer360 only links out to that same unfiltered/customer-filtered list. Resolved (2026-08-08): should be a real page formatted as an actual printable/downloadable invoice, following the same pattern already used by Purchase Orders and POS receipt reprints
  - [ ] The "collect the whole invoice, never per item" rule is already true in the data model (`ARInvoice` has no line-item breakdown at all) — but depends on Scenario 23's Closing Gap 5 to actually close the one real loophole (same-term items still generating separate invoices per product today)
- [ ] **24 — Area-Based Collector Assignment** — new (2026-08-08), developer-defined counter-check scenario from a POS/collections team meeting comment (not sourced from either client PDF), not started. [plan](./scenario-24-collector-area-assignment-plan.md)
  - [ ] No "Area" concept exists anywhere (schema-wide search for area/zone returns zero hits); `Customer.billingAddress`/`shippingAddress` are unstructured free text with no city/barangay column
  - [ ] Area = barangay (developer-confirmed 2026-08-08), keyed by `brgy_code` not name — barangay names repeat across different PH cities. The CRM customer form's `PhilippineAddressPicker` already resolves this data internally but discards it — only emits one flattened text string, so there's nothing downstream to derive Area from without rework
  - [ ] `Collector` has no geographic coverage beyond `branchId`; account assignment (`InstallmentAccount.collectorId`) is a flat, unfiltered manual pick from every collector regardless of the customer's location
  - [ ] Also folds in a related cleanup: `billingAddress`/`shippingAddress` are functionally one field already — every real customer-creation path mirrors one value into both — worth collapsing to a single `address` column before building area-structuring on top

## Notes

- Scenarios 15-21 were added 2026-07-31 from a second, independently-sourced client scenario map (`NIG_ERP_Core_Operational_Scenarios_Draft_2_20260727.pdf`) — see [module-scenarios.md](./module-scenarios.md)'s "Draft 2 additions" section for the full row-by-row routing table, including which of that PDF's 18 rows matched an existing scenario 1:1 (no new doc needed) versus required a new one.
- Two unresolved conflicting findings on **RFD/Application Form** printability: Scenario 01's check found a working `handlePrint()` in `ReleaseApprovalsList.tsx` (real client-side printable HTML); Scenario 02's check said the backend only returns a status label. Both can be true at once (frontend prints from the label's data) — worth a manual click-through if it matters.
- Several branches that looked "unmerged" (`feat/scenario-04-...`, `feat/scenario-08-caravan`, `feat/aircool-*`, `feat/scenario-12-cit-monitor`, etc.) are actually **stale**, not pending — their content already shipped to `development` via later commits and the branches were never cleaned up.
- Scenario 06 and 11's plan docs describe large gaps that are already closed in code — flagged for a docs refresh, not because the work is missing.
- Scenario 14 (2026-08-03): while closing the P&L branch/view gaps, found every seeded `Account` row has `category: null` (only `type` is populated), so `profitAndLoss()`/`balanceSheet()` return ₱0 for revenue/COGS/opEx against real data regardless of branch/view — pre-existing, unrelated to the scenario's own gaps, needs its own fix. Also `reports.controller.ts` has no permission/tenant guard at all (`Account`/`ARInvoice`/`APBill`/etc. have no `tenantId`), so the whole reports module — not just this scenario's additions — is readable by any authenticated user.
- The base `prisma/seed.ts` CLI seed never creates GL account 2060 (WHT Payable) or its account mapping — only the separate "Seed PH Defaults" endpoint (`coa-seed.service.ts`'s `seedPH()`) does. This has now silently broken a fresh-seed dev environment twice (2026-07-27, 2026-07-31); worth a real fix in `prisma/seed.ts` rather than repeated manual backfills.
- `inventory-receiving-branch-serial-gl.e2e-spec.ts` had a real test-ordering bug (fixed 2026-07-31): its one hard-closed-fiscal-period test creates a period covering the whole current month, torn down only at the very end of the file — any describe block placed after it that does a real-cost receive fails once the wall-clock date rolls into that period's month. Now positioned last in the file, with the reason documented inline. Its `afterAll` cleanup was also fixed (was deleting `Item` before `StockCostLayer`, violating the FK on every run).
- Frontend e2e specs in this codebase clean up fixtures inline at the end of each test body (not via `afterEach`) — if a test fails/throws partway through, its fixtures are silently orphaned in the dev DB (found 2026-08-03: a Scenario 20 test's own fixture from an earlier, since-fixed locator bug, still sitting in the Installment Accounts list days later, confusing manual QA). `customer.remove()` is a soft delete (`deletedAt`, filtered everywhere) so successful runs' cleanup is invisible either way — the risk is specifically failed runs, whose fixtures never reach the cleanup code at all. No fix applied (would mean reworking every spec's cleanup pattern) — just flagging so a stray-looking row prompts "check for a failed run," not a data bug.
- The shared local dev DB keeps re-dropping `items_name_trgm_idx` (Scenario 16's pg_trgm near-duplicate index) as spurious drift on every `migrate dev`/`migrate deploy`, since it's raw-SQL-only and not declared in `schema.prisma` — first hit during Scenario 16, hit three more times during Scenario 19 (caught and restored each time). New tables also land owned by `karmslajo` instead of `postgres` after a migration, needing a manual `ALTER TABLE ... OWNER TO postgres`. Both are the same underlying DB-tooling friction; worth a real fix (declare the extension/index properly, sort out DB ownership) rather than repeated manual recovery on every migration that touches this schema.
- `e2e/inventory-stock-adjustment.spec.ts` has a stale hardcoded fallback password (`dev-prominent-enterprise-2025`) that no longer matches the current dev-bypass password (`dev-prominent-enterprise-2026`, per `backend/.env`'s `DEV_API_KEY`) — fails against live Auth0 login unless `E2E_ROLE_PASSWORD` is set to override it. Found 2026-08-04 while verifying Scenario 19, unrelated to it; not fixed.
- `procurement-closeout.e2e-spec.ts`'s "GL posting — write-off" test posts to `/inventory/adjustments/write-off`, a route that has never existed (404) — Scenario 10 territory, unrelated to Scenario 19. The same file's "Spending quota enforcement"/"PO receipt linkage"/"Purchase Order lifecycle" tests are flaky under repeated same-day runs — a day-based sequential PO-code generator race, same class of bug as the `ADJ-`/`CNT-` number generators. Found 2026-08-04, not fixed.
- `repair-transfer.e2e-spec.ts`'s "auto-paired transfer dispatches and receives with no approval gate" test (Scenario 07) fails identically on a clean `development` checkout with no Scenario 19 changes applied (confirmed via `git stash`) — pre-existing, unrelated. Found 2026-08-04, not fixed.
- **2026-08-05 full re-audit, highest-severity finding**: Scenario 10's frontend AP UI (`VoucherPanel.tsx`, `SupplierDebitMemoDialog.tsx`, the `ap-payment-methods` page, PO document download) merged to `development` via PR #91, but the matching backend branch (`feat/scenario-10-purchasing-ap`, tip `5180eba`) was never merged — these are live, clickable features on `development` today that 404 on submit for any real user. Recommend fast-tracking that backend PR or feature-flagging the frontend surfaces until it lands. Scenario 13's supplier-debit-memo gap is the same root cause, not a separate bug. — **RESOLVED 2026-08-07**: the backend branch merged as PR #89 (`6fd7ffd`, 2026-08-05/06); re-verified every frontend call site against the real backend routes (path, HTTP method, DTO shape) for all 5 previously-broken surfaces plus match-check and the cheque document — all now match exactly, no remaining mismatches. Scenario 13's supplier-debit-memo 404 resolved the same way.
- Scenario 15's row previously said both PRs (#103 frontend, #99 backend) were "pending review/merge" — re-verified 2026-08-05 via `git merge-base --is-ancestor`, both are confirmed merged (`7a58bf9`, `044ab1b`); row flipped to closed. Separately, the plan doc's claim that `BranchPricing` is dead code is itself inaccurate — `branch-pricing.controller.ts`/`.service.ts` and a full frontend page (wired into the sidebar) exist and predate the plan doc; it's a parallel pricing mechanism, not dead.
- Scenario 19's parent checkbox was left unchecked despite the plan doc's own log showing full implementation (2026-08-04) — re-verified 2026-08-05, flipped to closed; only the explicitly-deferred serial-level item remains open. (Separately, and NOT part of this audit per instruction: a local, uncommitted `feat/scenario-19-...-v2` branch with further stock-count work — branch scoping — is currently mid-merge and untouched in both repos' working copies.)
- Scenario 20's plan doc still listed "ReminderController/InteractionController have no PermissionsGuard at all — any authenticated user can create/complete/delete any reminder or interaction" as an open risk. Re-verified 2026-08-05: fixed in commit `213cb81`, which also granted Branch Manager/Cashier the CRM permissions they'd need to keep working once that guard was enforced (they'd previously been granted none), and added collections context (customer/lead/installment-account label) to the generic reminders list — closing another "worth flagging" item from the same doc. Plan doc's own Implementation Log needs updating to reflect this; checklist row was already accurate since it only tracked the DAM chain item as open.
- Scenario 18's Quarantine + inspection step (Part 1) is fully built and ready on unmerged branch `feat/scenario-18-returns-exchanges-disposition` (`b91c9df`) — genuinely not on `development` yet, so "not started" remains correct, but it's not untouched work either.
- Scenario 11: found a real, previously-untracked gap — `recordPayment()` is still a single direct action with no OR-stub/remittance two-step workflow (the original plan doc's Closing Gap #4). Added as a new sub-item. **Re-checked 2026-08-07**: a new POS Collections screen merged this pass (branch/collector/payment-method capture on AR payments, same-day duplicate guard, collections customer list — PR #108/#115) — adjacent and related, but it's still a single-step `recordPayment()` under the hood; doesn't touch the OR-stub/remittance gap, which remains open.
- The RBAC self-escalation bugs tracked separately (`RolesController` has no `PermissionsGuard`; `POST /users` has no permission check) were re-confirmed still live and unpatched on `development` as of 2026-08-05 — now cross-referenced under Scenario 21, since any maker≠checker/approval-limit design there would inherit the same hole. **Clarified 2026-08-07**: `RolesController` already carries `@RequirePermissions(...)` decorators on every route — this is a pre-existing state, at least as old as 2026-07-19 per prior investigation, not something that changed this pass — but `PermissionsGuard` (the guard that actually reads those decorators) is never applied to the controller and isn't registered globally (`app.module.ts` has no `APP_GUARD` provider for it, `main.ts` has no `useGlobalGuards()`), so nothing enforces them: every roles endpoint is still callable by any authenticated user. `POST /users` is unchanged and still unguarded. Worth flagging to whoever is expected to fix this — the decorators read as done at a glance, aren't, and could get checked off by mistake.
- This pass's audit was run against clean, isolated `git worktree` checkouts of `origin/development` in both repos, not the local working copies — both local checkouts are currently mid-merge on the unrelated, unfinished `feat/scenario-19-stock-count-adjustment-approval-v2` branch (per developer instruction, left untouched and not inspected).
- **2026-08-07 targeted re-audit** (prompted by a large volume of merges to `origin/development` in the 2 days since the last full pass — see updated "Last verified" line at the top): checked all rows against `origin/development` HEAD directly rather than the local working copies, since both had diverged from `development` again — backend is clean and on `development` but its HEAD had moved on; frontend is on an unrelated branch `fix/financing-term-edit-delete` with 6 uncommitted files (`ReceiveStockModal.tsx`, `ItemSearchCombobox.tsx`, `pos-actions.ts`, `usePos.ts`, `FinancingTermList.tsx`, `schema/pos/index.ts`) — left untouched, not inspected, not part of this audit.
- **2026-08-07**: found a new unmerged branch, `feat/inventory-transfer-partial-receipt` (backend) with a matching frontend branch, adding partial/reopenable stock transfer receiving (a transfer can be received in installments and stays open until fully received, plus a shared variance badge). This isn't a gap under any tracked scenario — Scenario 06 (Stock Request & Inter-Branch Transfer) is already closed and this is an enhancement on top of it, not a fix to a known gap — flagging only because it's live WIP that doesn't show up anywhere else in this doc.
- **2026-08-07**: Scenario 17's unmerged branch (`feat/scenario-17-credit-application-promissory-note`, same name both repos) has grown substantially since 2026-07-31: full `CreditApplication`/`PromissoryNote`/investigation schema and API, a seeded Credit Investigator role with `credit:*` permissions, the installment-sale gate requiring an approved application + signed note, a complete frontend Credit module (moved under the POS route), promissory-note print/sign fallback fixes, and a doc closing the scenario out — none of it is on `origin/development` yet (confirmed via `git merge-base --is-ancestor` on several tip commits from both repos), so the checklist row is unchanged, but this is not a cold-start scenario anymore.
