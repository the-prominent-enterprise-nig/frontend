# Scenario Checklist — Implementation Status

One-page status for all module scenarios (14 from the original source, plus 15-21 added 2026-07-31 from a second client scenario map — see [module-scenarios.md](./module-scenarios.md)'s "Draft 2 additions" for the full routing table — 22-25 added individually between 2026-08-07 and 2026-08-08, and 26 added 2026-08-12, as new gaps/counter-checks surfaced live). Each row is re-verified against the actual `development` branch code in both repos (not just the plan docs' own checkmarks, which can drift) — see the full gap analysis and Implementation Log in each scenario's own plan doc for file/line evidence.

Last verified: 2026-08-13 (Scenario 26 same-day follow-up — after Phase 1 + Phase 2 closed, a dedicated audit of POS/Inventory for unnotified maker-checker actions found and closed 4 more gaps: SKU Reservation cancellation, Price List approval (plus its `createdById` schema blocker), Batch quality-hold disposition, and Purchase Request→PO (moved out of Phase 3 deferral) — including a second RBAC segregation-of-duties fix, same shape as the Stock Adjustment one from earlier the same day. All backend-only, tested (new Jest e2e specs per workflow), and confirmed. See the plan doc's second 2026-08-13 Implementation Log entry for full detail.)

Previously verified 2026-08-13 (Scenario 26 — Phase 1 + Phase 2 (all 9 parts) implemented on `feat/scenario-26-notification` in both repos, tested (Jest e2e + Playwright) and manually confirmed by the developer across extensive live testing. See the plan doc's Implementation Log for detail, including a real RBAC segregation-of-duties fix (Stock Controller/Branch Manager's `inventory:*` wildcards over-granting past their intended stage in the Stock Adjustment approval chain) and a stock-count-to-adjustment creation path that was silently bypassing notification wiring entirely — both found via the developer's own live testing, not this scenario's original scope.)

Previously verified 2026-08-10 (Scenario 09 — Aircool's Closing Gap 6 (type of service at job creation) implemented end-to-end on `feat/aircool-service-type-and-invoice-link` in both repos, plus a new developer-requested follow-up wiring the previously-unused `ServiceDraft.posTransactionId` field to a real picker/display (linking a service job to its originating sale/invoice). Both parts tested (Jest e2e + Playwright) and manually confirmed by the developer. Scenario 09 is now fully closed. See the plan doc's Implementation Log for detail.)

Previously verified 2026-08-10 (Scenario 01 reopened and closed on `feat/scenario-01-installment-down-payment-floor` in both repos — the 10% minimum down payment floor closing gap done, tested, and manually confirmed by the developer; see the plan doc's Implementation Log for detail, including two live-testing follow-up bugs fixed in the same area — a misleading "Nothing to collect" state now resolved by auto-filling the down payment at the floor, and a bare "Preview unavailable." now surfacing the real backend error — plus a floating-point precision fix to the floor check itself.)

Previously verified 2026-08-10 (Re-verification pass on the merged `development` HEAD — frontend `907fcda`, backend `056bcb5` — dispatched as 5 parallel checks specifically to catch errors from the prior full re-audit's manual `git stash pop` merge-conflict resolution. 24 of 25 scenarios confirmed clean and accurate, no merge-conflict artifacts found anywhere in the doc. One real drift found and fixed: Scenario 19's "serial-level count reconciliation — explicitly deferred" sub-item was stale — it actually shipped the same day the scenario closed. See Notes for full detail on this and all earlier passes.)

- [x] **01 — POS Installment Sale** — fully closed. (plan doc removed 2026-08-14 — scenario fully closed, no open items)
- [ ] **02 — CRM Customer Profile** — core done (2026-08-01: co-maker, duplicate detection, BM/AR merge-resolution, ID/consent capture, Lead-conversion loyalty bug); segment campaigns remain out of scope pending their own integration/scoping pass. [plan](./scenario-02-crm-customer-profile-plan.md) / [updates](./scenario-02-crm-customer-profile-updates.md)
  - [ ] Segment → campaign send mechanism doesn't exist
- [x] **03 — Reservation / Advance Sale** — fully closed. (plan doc removed 2026-08-14)
- [x] **04 — POS Cross-Branch Serial** — fully closed. (plan doc removed 2026-08-14)
- [ ] **05 — Receiving** — core gaps + both updates-doc items fixed, 2 narrow follow-ups open. [plan](./scenario-05-receiving-plan.md) / [updates](./scenario-05-receiving-updates.md)
  - [ ] Cost-view restriction only covers the Receiving flow — Item Master, Costing/Valuation, Bundles, and PO screens still show cost to everyone (14 files, deferred; Scenario 16, its originally-suggested owner, is now closed and its doc removed — this item needs a new home or its own scoping pass)
  - [ ] Freebies flag only exists in the standalone Receiving form — "Receive against PO" has no way to add an extra (unordered) freebie line
- [x] **06 — Stock Request & Inter-Branch Transfer** — fully closed, including the region-based warehouse-leg model added by Scenario 27. (plan doc removed 2026-08-14 — was fully superseded by Scenario 27's transfer changes, not just stale)
- [x] **07 — Repair Transfer** — fully closed, all 5 parts. (plan doc removed 2026-08-14)
- [x] **08 — Caravan** — fully closed. (plan doc removed 2026-08-14)
- [x] **09 — Aircool (aircon sale + install)** — fully closed 2026-08-10. (plan doc removed 2026-08-14)
- [ ] **10 — Purchasing & Accounts Payable** — 2026-08-05's live-404 breakage is resolved: backend (`feat/scenario-10-purchasing-ap`, #89, `6fd7ffd`) merged 2026-08-05/06 and the frontend AP UI now wires up correctly end-to-end (verified route-for-route, DTO-for-DTO). PR→PO→RR, voucher approval, 3-way match, cheque printing, supplier debit memos, AP payment methods (incl. GL mapping), PO document download, PO freebies (Part 8), and PO-line supplier discounts are all live and functioning. Re-verified 2026-08-10: the voucher-attachments item below was stale; one narrow gap remains. [plan](./scenario-10-purchasing-ap-plan.md) / [updates](./scenario-10-purchasing-ap-updates.md)
  - [ ] Auto 1% withholding on payment — `RecordAPPaymentDto.withholdingAmount` is still a plain optional number the caller supplies, no automatic 1% computation anywhere in `ap-bills.service.ts`; defaults to 0 if omitted (a separate, unrelated 1%-withholding auto-calc already exists on the Receiving/RR side in `ReceiveStockModal.tsx` — doesn't close this gap)
  - [x] ~~Voucher attachments~~ — resolved, corrected 2026-08-10: `CreateVoucherDto` (`ap-bills.dto.ts:200`) still only has `voucherNumber`, but attachments were built through the pre-existing generic `FileAttachments` entity-link subsystem instead — `VoucherPanel.tsx` uploads via `upload-voucher-attachment.ts` then calls `FileAttachments.attach(fileId, 'APBill', bill.id)`, backend `file-attachments.controller/service.ts` (entity-agnostic). Frontend `681705f` 2026-08-04 (#91). The doc's DTO-only check had missed this mechanism.
- [x] **11 — Collections & AR Aging** — doc was very stale; a real Collector/InstallmentAccount module already shipped. [plan](./scenario-11-collections-ar-aging-plan.md)
  - [ ] (minor) Generic AR aging report (`reports.service.ts`) still flat/single-clock, not integrated with the new collections aging
  - [ ] No two-step _ledger-gated_ collection-receipt workflow — `InstallmentAccount.recordPayment()` (`installment-account.service.ts:731-766`) still posts directly to `currentBalance` with no pending/confirm step. Reworded 2026-08-10: a `CollectorRemittance` cash-remittance log DOES exist (`POST /crm/collectors/:id/remittances`, since 2026-07-28 — predates this item's original "found 2026-08-05" note) but it's a disconnected lump-sum log, not linked to individual payments/OR numbers and not used as a pending-confirm gate. The deeper gap the plan doc describes (original Closing Gap #4) is still genuinely open.
- [x] **12 — EOD Cash & Cash-in-Transit Monitor** — fully closed, incl. company-wide monitor + Excel export. (plan/updates docs removed 2026-08-14)
- [x] **13 — Credit & Debit Memos** — fully closed 2026-08-10, all 5 closing-gap items. (plan doc removed 2026-08-14)
- [x] **14 — Accounting Daily & Month-End** — fully closed (2026-08-03: tax-rate approver-gating, per-branch P&L, internal-vs-net P&L view, cost-center report). (plan doc removed 2026-08-14)
- [x] **15 — Price List Management & Approval** — fully closed: branch scoping, approval workflow, floor-price validation, versioning (`supersedesId`, with a UI picker), all 8 price-use-types, the DP/term/installment linkage schema + full checkout wiring, and the real NIG price data import (42/686 catalog-matched products, 275 curated price-list-item rows across 7 price-use-types) are all done, merged, and verified against a fresh reseed. Re-verified 2026-08-10: the 4 sub-items below were stale leftovers from the pre-followup gap list — checked off against current code (`schema.prisma:5106-5163`, `seed.ts`, `pos.service.ts:2770-3038`). [plan](./scenario-15-price-list-management-plan.md)
  - [x] ~~DP/term/installment linkage~~ — `PriceListItem.downPayment`/`cmAmount`/`creditAmount` + child table `PriceListItemTerm` (`termMonths`/`monthlyInstallment`/`ppd`) live in schema; curated rows win over `FinancingTerm.factorRate` at both checkout preview and posting
  - [x] ~~Three price-use-types not seeded~~ — TONIK, SKYRO, CREDIT CARD all seeded (`seed.ts`), price-only rows, no installment breakdown, matching confirmed scope
  - [x] ~~Coded PPD rounding mismatch~~ — real PPD values imported from the source rate card and stored on `PriceListItemTerm`, overriding the live 7.5% formula (`ppdOverride`) wherever curated data exists; formula remains only as the no-curated-data fallback
  - [x] ~~Open question: CM/CREDIT columns~~ — resolved with the developer: sales commission, varies by price-use-type; captured as real fields (`cmAmount`/`creditAmount`), wiring into `AgentCommission` deliberately deferred as a separate follow-up (not a blocker)
- [x] **16 — Item Master Governance** — fully closed (2026-08-04): draft→submit→confirm-accounting→approve workflow, Master Data Approver role, pg_trgm near-duplicate warning. (plan doc removed 2026-08-14)
- [x] **17 — Credit Application, Investigation & Promissory Note** — fully closed (2026-08-05, hardened 2026-08-06). (plan doc removed 2026-08-14)
- [ ] **18 — Customer Returns, Exchanges & Disposition** — not started on `development` (re-confirmed 2026-08-05); Part 1 (Quarantine + inspection step) is built and ready on unmerged branch `feat/scenario-18-returns-exchanges-disposition` (`b91c9df`), just not landed yet. [plan](./scenario-18-returns-exchanges-disposition-plan.md)
  - [ ] No Quarantine hold, no tiered custodian+approver flow, Exchange is a vestigial enum value, not connected to `CreditMemo`
- [x] **19 — Stock Count & Inventory Adjustment Approval** — fully implemented, confirmed closed 2026-08-05. (plan doc removed 2026-08-14)
- [x] **20 — Collections Reminder Track (NAMIDRe) & Delinquency Escalation (DAM)** — fully closed 2026-08-03. (plan doc removed 2026-08-14; note: a formal multi-stage BM→Area Supervisor→HO AR legal-escalation review chain was deliberately left out of scope, not a gap)
- [ ] **21 — Role-Based Action Queues, Maker-Checker & Approval Limits** — new (2026-07-31), not started. [plan](./scenario-21-role-queues-maker-checker-plan.md)
  - [ ] `PendingApprovalsWidget` is hardcoded mock data, no maker≠checker enforcement, `AccountingAuditLog` unused, no approval limits
  - [ ] Offline sync explicitly deferred — flagged as future work, not scoped into this doc's implementation
  - [x] ~~Blocking prerequisite risk~~ — RESOLVED, corrected 2026-08-10: this item was accurate as of its 2026-08-07 recheck but went stale the very next day. `RolesController` (`roles.controller.ts:25`) is now class-level `@UseGuards(JwtAuthGuard, PermissionsGuard)` and `POST /users` (`users.controller.ts:141-142`) now requires `admin:users:create` under `PermissionsGuard` — both landed in commit `eee202c8` (2026-08-08, Scenario 22's own fix), merged to `development` via `2dc4996`/PR #112 (2026-08-10). Residual, narrower risk: `CreateUserDto.roleIds` is still caller-supplied with no check on _which_ roles the caller may grant — anyone holding `admin:users:create` could still hand out Business Owner. Worth its own follow-up, but the original wide-open hole is closed, and any maker≠checker/approval-limit design here no longer inherits it.
- [ ] **22 — Fixed Role Hierarchy, Module Access & Protected Routes** — new (2026-08-07). Re-verified 2026-08-10: checklist was stale — 6 of 7 closing-gap items are actually already fixed on `development` (landed via `feat/scenario-22-rbac-hardening`, merged per this branch's own git log). Only the frontend `/403` sweep remains open. [plan](./scenario-22-fixed-roles-protected-routes-plan.md)
  - [x] ~~Self-escalation bugs~~ — `RolesController` now class-level `@UseGuards(JwtAuthGuard, PermissionsGuard)` (`roles.controller.ts:25`); `POST /users` now `@RequirePermissions('admin:users:create')` under `PermissionsGuard` (`users.controller.ts:140-142`)
  - [x] ~~Founding Business Owner protection~~ — real `User.isFounder` column (migration `20260807120000_add_user_is_founder`), enforced via `assertNotTargetingFounderAsOther` in `users.service.ts` (`removeRole`/`remove`/`setActiveStatus`/`update`/`assignRole`/branch changes all call it), plus the "neuter via the role's own permissions" side door closed separately (`roles.service.ts`'s `assertNotFounderRole`)
  - [x] ~~`Stock Controller` vs `Inventory` duplication~~ — consolidated to `Stock Controller` alone, granted full `inventory:*` (`seed.ts:3070-3239`); no separate `Inventory` role remains
  - [x] ~~Role name protection~~ — server-side `PROTECTED_ROLE_NAMES` now enforced in `RolesService.update()`/`remove()` (`roles.service.ts:20-27`), not just the frontend's UI-only copy
  - [x] ~~`Role.isActive` enforcement~~ — now filtered at the point permissions are read (`jwt.strategy.ts:215-216`, `permission-check.util.ts:48`); the old dead `CurrentUserGuard` file was deleted outright rather than left unused
  - [ ] Protected-route enforcement (`/403`) — **backend closed** (every controller under `pos/`, `accounting/`, `crm/` now has `PermissionsGuard`, no global `APP_GUARD` but per-controller coverage is complete), **frontend still opt-in and inconsistent**: CRM 23/24 pages and Inventory 38/39 have the `/403` guard, but Accounting only 3/23, POS only 10/29, Settings 9/12, Workspace 0/2 — e.g. `accounting/journal-entries`, `accounting/ar-invoices`, `pos/transactions`, `pos/checkout` are all still reachable by direct URL with no access-denied page, even though the API underneath now correctly 403s
- [x] **23 — POS Transaction ↔ Invoice Lookup Consistency** — fully closed 2026-08-09, all 5 closing-gap items. (plan doc removed 2026-08-14)
- [x] **25 — Per-Invoice Detail View & Whole-Invoice Collections** — fully closed 2026-08-10, all 3 closing-gap items. (plan doc removed 2026-08-14)
- [x] **24 — Area-Based Collector Assignment** — fully closed 2026-08-10, all 4 closing-gap items. (plan doc removed 2026-08-14; note: retroactive backfill of existing free-text addresses to barangays was deliberately not attempted — not reliably automatable)
- [ ] **26 — In-App Notification System (Approval Workflows & Operational Alerts)** — Phase 1 + Phase 2 fully closed 2026-08-13, plus a same-day follow-up round closing 4 more gaps found via a dedicated POS/Inventory audit: SKU Reservation cancellation, Price List approval, Batch quality-hold disposition, and Purchase Request→PO (moved out of Phase 3 deferral). Real-time notification bell with per-user WebSocket delivery — every wired workflow notifies the real per-stage actor (not a fixed Owner/Branch-Manager pair) and the original submitter on resolution. See the plan doc's two 2026-08-13 Implementation Log entries for detail, including a real RBAC segregation-of-duties fix applied twice (Stock Adjustment, then Purchase Request/Order) — Stock Controller's `inventory:*` wildcard let it confirm/approve/reject past its intended stage in both chains — and a stock-count-to-adjustment creation path that was silently bypassing notification wiring entirely. [plan](./scenario-26-notification-system-plan.md)
  - [ ] Phase 3 not started — Inter-branch Transfers, Repair Transfer, DAM Escalation/Graduation, AP Bills Voucher, Tax Rate Change Request; AP Bills additionally blocked on a requester-tracking schema addition (Price List's own half of that same gap closed 2026-08-13 alongside its approval-needed/resolved wiring)
  - [ ] PO `cancel()` (a withdrawal, not an approver rejection) deliberately left unwired this same-day follow-up pass — only `create()`/`convertFromPr()`/`approve()` notify
  - [ ] Batch quality-hold's maker/checker permission is still a single shared grant (`inventory:batch:manage` gates both placing and releasing a hold) — notification wiring shipped 2026-08-13 regardless, but the underlying RBAC split was explicitly left as-is by developer choice
- [ ] **27 — Warehouse Tier Correction (Procurement → Warehouse → Branch Distribution)** — new (2026-08-11), all 5 closing gaps structurally done as of 2026-08-12; left unchecked because of real follow-up spillover this scenario's own work created (see bottom). Client corrected the "1 warehouse per branch" assumption behind the Aug 10 relabel: only 2 real warehouses exist (PANAY, NEGROS), and stock should flow Main Office → Warehouse (supplier) → Branch, via a **new flow kept fully separate from Branch↔Branch Transfers** (branch pull request or warehouse push delivery, hard-capped against live warehouse stock, region-defaulted with manual override). **Part 1 done 2026-08-11**: PANAY/NEGROS exist as real standalone warehouses — turned out 2 of the 41 seeded branches (`NWHSE`/`PWHSE`) were already meant to be these, promoted rather than creating new ones, now tagged `type: warehouse`. Warehouses admin page filtered to show only the 2 real ones; 23 other Inventory pickers/filters/columns fixed for the same raw-warehouse-name issue. **Part 2 done 2026-08-12**: `Branch.region` backfilled for all 41 branches, new `WarehousesService.resolveRegionalWarehouseForBranch()`. **Part 3 done 2026-08-12**: PO receiving and manual receiving destination pickers both limited to the 2 real warehouses; backend `receiveStock()`'s stale branch-ownership check replaced with a real-warehouse integrity check. **Part 4 (a/b/c) done 2026-08-12** ("Warehouse Requests"): new `WarehouseRequest`/`WarehouseRequestLine` models, fully separate from `StockTransfer`; lighter lifecycle (`requested→ready/rejected→in_transit→received`, `cancel` before dispatch); Head Office can also create a pull request (explicit branch picker, same pattern as PO creation); warehouse-side authorization (accept/reject/dispatch) is region-matching (any Stock Controller in the warehouse's region), branch-side (`receive`) is exact-branch-only; hard cap enforced at creation and re-checked at dispatch. Full UI on both sides: `/inventory/warehouse-requests` (create pull/push, list, receive, cancel, accept, reject-with-reason, dispatch), linked from both the Inventory dashboard's quick-link grid and the main sidebar (initially only the former — developer caught it wasn't actually discoverable, sidebar entry added same day). Found and fixed 2 real bugs while building the frontend: a missing detail-query cache invalidation on cancel (modal showed stale status, caused a real double-submit in the e2e spec before the fix — since generalized into one shared `invalidateRequestQueries()` helper so no new action can repeat it), and flagged-not-fixed a separate pre-existing bug in `warehouses.service.ts`/`useWarehouseManager.ts` (nested `meta` response read as flat, breaks the Warehouses admin page's pagination — unrelated to this scenario, out of scope). **Part 5a/5b done 2026-08-12** — serial-number correctness + the 4c two-button design superseded. Developer flagged every item in NIG's real catalog is serial-tracked, and the original design never moved individual `SerialNumber` rows at all (only aggregate `StockBalance`) — a real data-integrity gap. **5a (backend)**: `WarehouseRequestLine` gets `serialNumberId` + `receivedQuantity`; new `partially_received` status. `create()` validates each serial exactly like Transfer does; `dispatch()` live-re-checks each specific serial (not just aggregate count) right before consuming it; `receive()` changed to a per-line checklist — confirmed units move the serial to the branch's warehouse, unconfirmed ones flag `lost_in_transit` and the request lands on `partially_received`. One-shot receive, not reopenable (kept simpler than Transfer's own partial-receive on purpose). E2e: 16/16. **5b (frontend)**: collapsed "New Request"/"New Delivery" into one button/form — `direction` is no longer client input, the server derives it (own branch → always `requested`; a region-matched Stock Controller creating for a DIFFERENT branch → self-approves `ready`) — verified this exact rule reproduces every existing test's expectations unchanged. New multi-serial "Add Item" picker (`SerialMultiPicker.tsx`) — pick an item once, multi-select as many in-stock serials as needed, auto-expands into grouped, individually-removable lines. Also had to patch the "Confirm Received" button (broken by 5a's new required receive body) to auto-build a full-receipt payload, keeping the one-click UX working until 5c's real per-serial checklist lands. E2e: 6/6 (the old dedicated push-button test removed — no longer reachable via UI from a branchless login; multi-serial-picker interaction itself has no dedicated e2e coverage yet, flagged not silently skipped). Live-testing same day caught 2 more real bugs, both fixed: the item picker needing an extra click before showing anything (now auto-opens once branch/warehouse are picked), and the manual-quantity fallback path never populating its display label (rendered as a raw UUID + a literal "x0" placeholder). **Part 5c done 2026-08-12**: replaced the 5b compatibility patch with the real per-serial receive checklist — "Receive" opens an inline form (every line defaults to checked/arrived, uncheck anything missing, struck-through for clarity), "Confirm Receipt" submits exactly that. E2e: 7/7, including the first frontend test to reach "Partially Received" through the real UI (not just direct API calls). **Closing Gap 3 addendum, 2026-08-12**: manually testing the full "buy from a supplier" flow (PR→PO→receive) surfaced that Part 3 only fixed the _receiving_ destination picker — PO _creation_ still had a "Branch" field (`CreatePoModal.tsx`: bound to `branchId`, with `warehouseId` unrendered entirely; `ConvertPrToPoModal.tsx`: mislabeled "Branch" but actually bound to `warehouseId`, from an unfiltered ~43-warehouse list). Fixed: both now show a required "Warehouse" field restricted to the 2 real ones; `ReceiveAgainstPoModal.tsx`'s "Destination Warehouse" is now locked/read-only once the PO already has one, only falling back to editable for a pre-fix legacy PO with none. 3 existing e2e specs (`purchase-order-{pdf,freebie,discount}.spec.ts`) needed a warehouse-pick step added since the field is now required — all 3 re-verified passing. **Closing Gap 3 addendum 2, 2026-08-12**: re-testing PO creation hit a live P2002 crash ("cannot create PO") — the count()-based sequence-generator bug (documented elsewhere in this scenario's own work) also existed in `purchase-order.service.ts`/`purchase-request.service.ts`; fixed max-based in both plus 3 more instances of the same bug found proactively (`stock.service.ts` GRN codes, `counts.service.ts` count numbers, `service-drafts.service.ts` invoice numbers). Regression run revealed 3 separate, pre-existing, unrelated `procurement-closeout` test failures — flagged only. **Data reconciliation, 2026-08-12**: client rule "every item needs a serial" surfaced most of the seeded catalog didn't have `isSerialTracked: true`. Flipped 7 real-physical-product items (Universal Remote Control deliberately excluded, 16-spec blast radius); found 6 of them seeded at a uniform 200 units across ~43 locations — fabricating matching serials at that scale (~48k rows) was too slow for a dev baseline, so reduced every location to a lean 20-unit baseline first, then fabricated exactly matching serials on top (verified `StockBalance` sums match `SerialNumber` counts exactly). `prisma/seed.ts` itself is unchanged — a fresh environment still seeds the old 200-unit/non-tracked state; flagged for a future client decision. **Retirement, 2026-08-14**: Part 4's whole Warehouse Request feature (and Part 5's serial-correctness work built on top of it) was retired one day after shipping — client's call: `StockTransfer`'s warehouse picker already includes the 2 real warehouses, so a dedicated warehouse↔branch mechanism was redundant once Transfer was brought up to parity. Ported into `transfers.service.ts` first (region-based warehouse-side auth, skip Manager/HQ approval for a warehouse leg, live per-serial re-check at dispatch — new dedicated coverage `stock-transfer-warehouse-leg.e2e-spec.ts`, 4/4), verified zero regressions against the full existing Transfer suite, then removed: both `WarehouseRequest*` Prisma models (real `DROP TABLE` migration), the whole backend feature slice, the whole `/inventory/warehouse-requests` frontend route, nav entries, permissions, and both e2e spec files. Full detail in the plan doc's new "Retirement" section.

E2e: the original `test/inventory-warehouse-requests.e2e-spec.ts` (backend) + `e2e/inventory-warehouse-requests.spec.ts` (frontend) were deleted with the Part 4/5 retirement; current coverage for the warehouse-leg flow is `test/stock-transfer-warehouse-leg.e2e-spec.ts` (backend, 4/4). **Still open, not pre-existing** — real spillover this scenario's own Part 3/4 changes created, tracked but not yet fixed: (1) SKU Reservation earmarking's post-receive hook still no-ops for every receipt (Part 3 finding) — Part 4's branch-side `receive()` is the new natural trigger point the plan doc predicted, but nothing wires it up yet; (2) the same branch-ownership-check pattern Part 3 fixed in `receiveStock()` is unverified in 5 other services (`sku-reservations`, `adjustments`, `counts`, `reservations`, `transfers`); (3) ~10 Inventory surfaces still show raw warehouse names because their services don't resolve branch names (Adjustments, Stock Balance, PO Receipts, Stock Counts, Reservations, Revaluation). [plan](./scenario-27-warehouse-tier-correction-plan.md)

- [x] ~~Seed data still creates 41 shadow warehouses 1:1 with branches, no real PANAY/NEGROS warehouse rows exist anywhere~~ — done (Part 1)
- [x] ~~`Branch` has no region/island field — needed to auto-pair a branch to its home warehouse~~ — done (Part 2)
- [x] ~~PO/goods-receiving destination picker (`ReceiveAgainstPoModal.tsx`) still says "Destination Branch", needs to go back to real "Warehouse" (2 options)~~ — done (Part 3)
- [x] ~~No warehouse↔branch request/delivery mechanism exists at all~~ — backend done (Part 4a); frontend UI not started (Part 4b/4c)
- [ ] Nothing yet filters `type: warehouse` branches (NWHSE/PWHSE) out of ordinary branch pickers elsewhere in the app (POS terminal assignment, branch dropdowns, etc.) — real Part 4 territory
- [ ] ~10 Inventory surfaces (Adjustments, Stock Balance columns, PO Receipts, Stock Counts, Reservations, Revaluation history) still show raw warehouse names — needs a batched branch-lookup added to their own backend services first, not a simple rename (see plan doc Closing Gap 1, Addendum 4)
- [ ] **Accepted gap**: SKU Reservation auto-earmarking-on-receipt (Scenario 03 Part 4) is now permanently a no-op — receiving destinations are never branch-owned anymore, and the earmarking hook only fires for a branch-owned warehouse. Needs Part 4 to give it a new trigger point (a branch receiving a distribution). `test/sku-reservation-earmarking.e2e-spec.ts` is skipped, not deleted, pending that.
- [ ] **Not verified**: the same branch-ownership-check pattern `receiveStock()` used to have also exists in `sku-reservations.service.ts`, `adjustments.service.ts`, `counts.service.ts`, `reservations.service.ts` — each will hit the identical failure mode the moment a branch-scoped actor's target becomes a real warehouse instead of a branch-local one. **Resolved for `transfers.service.ts` specifically** — the 2026-08-14 Part 4 retirement ported this same fix there as `assertWarehouseScope()` (region-based, not branch-ownership), verified 2026-08-14; drop it from this "not verified" list.

## Notes

- Scenarios 15-21 were added 2026-07-31 from a second, independently-sourced client scenario map (`NIG_ERP_Core_Operational_Scenarios_Draft_2_20260727.pdf`) — see [module-scenarios.md](./module-scenarios.md)'s "Draft 2 additions" section for the full row-by-row routing table, including which of that PDF's 18 rows matched an existing scenario 1:1 (no new doc needed) versus required a new one.
- Two unresolved conflicting findings on **RFD/Application Form** printability: Scenario 01's check found a working `handlePrint()` in `ReleaseApprovalsList.tsx` (real client-side printable HTML); Scenario 02's check said the backend only returns a status label. Both can be true at once (frontend prints from the label's data) — worth a manual click-through if it matters.
- Several branches that looked "unmerged" (`feat/scenario-04-...`, `feat/scenario-08-caravan`, `feat/aircool-*`, `feat/scenario-12-cit-monitor`, etc.) are actually **stale**, not pending — their content already shipped to `development` via later commits and the branches were never cleaned up.
- Scenario 06 and 11's plan docs describe large gaps that are already closed in code — flagged for a docs refresh, not because the work is missing.
- Scenario 14 (2026-08-03): while closing the P&L branch/view gaps, found every seeded `Account` row has `category: null` (only `type` is populated), so `profitAndLoss()`/`balanceSheet()` return ₱0 for revenue/COGS/opEx against real data regardless of branch/view — pre-existing, unrelated to the scenario's own gaps, needs its own fix. Also `reports.controller.ts` has no permission/tenant guard at all (`Account`/`ARInvoice`/`APBill`/etc. have no `tenantId`), so the whole reports module — not just this scenario's additions — is readable by any authenticated user.
- The base `prisma/seed.ts` CLI seed never creates GL account 2060 (WHT Payable) or its account mapping — only the separate "Seed PH Defaults" endpoint (`coa-seed.service.ts`'s `seedPH()`) does. This has now silently broken a fresh-seed dev environment twice (2026-07-27, 2026-07-31); worth a real fix in `prisma/seed.ts` rather than repeated manual backfills.
- `inventory-receiving-branch-serial-gl.e2e-spec.ts` had a real test-ordering bug (fixed 2026-07-31): its one hard-closed-fiscal-period test creates a period covering the whole current month, torn down only at the very end of the file — any describe block placed after it that does a real-cost receive fails once the wall-clock date rolls into that period's month. Now positioned last in the file, with the reason documented inline. Its `afterAll` cleanup was also fixed (was deleting `Item` before `StockCostLayer`, violating the FK on every run).
- Frontend e2e specs in this codebase clean up fixtures inline at the end of each test body (not via `afterEach`) — if a test fails/throws partway through, its fixtures are silently orphaned in the dev DB (found 2026-08-03: a Scenario 20 test's own fixture from an earlier, since-fixed locator bug, still sitting in the Installment Accounts list days later, confusing manual QA). `customer.remove()` is a soft delete (`deletedAt`, filtered everywhere) so successful runs' cleanup is invisible either way — the risk is specifically failed runs, whose fixtures never reach the cleanup code at all. No fix applied (would mean reworking every spec's cleanup pattern) — just flagging so a stray-looking row prompts "check for a failed run," not a data bug.
- The shared local dev DB keeps re-dropping `items_name_trgm_idx` (Scenario 16's pg_trgm near-duplicate index) as spurious drift on every `migrate dev`/`migrate deploy`, since it's raw-SQL-only and not declared in `schema.prisma` — first hit during Scenario 16, hit three more times during Scenario 19 (caught and restored each time). New tables also land owned by `karmslajo` instead of `postgres` after a migration, needing a manual `ALTER TABLE ... OWNER TO postgres`. Both are the same underlying DB-tooling friction; worth a real fix (declare the extension/index properly, sort out DB ownership) rather than repeated manual recovery on every migration that touches this schema.
- `e2e/inventory-stock-adjustment.spec.ts` has a stale hardcoded fallback password (`dev-prominent-enterprise-2025`) that no longer matches the current dev-bypass password (`dev-prominent-enterprise-2026`, per `backend/.env`'s `DEV_API_KEY`) — fails against live Auth0 login unless `E2E_ROLE_PASSWORD` is set to override it. Found 2026-08-04 while verifying Scenario 19, unrelated to it; not fixed.
- `procurement-closeout.e2e-spec.ts`'s "GL posting — write-off" test posts to `/inventory/adjustments/write-off`, a route that has never existed (404) — Scenario 10 territory, unrelated to Scenario 19. The same file's "Spending quota enforcement"/"PO receipt linkage"/"Purchase Order lifecycle" tests are flaky under repeated same-day runs — a day-based sequential PO-code generator race, same class of bug as the `ADJ-`/`CNT-` number generators. Found 2026-08-04, not fixed.
- `repair-transfer.e2e-spec.ts`'s "auto-paired transfer dispatches and receives with no approval gate" test (Scenario 07) fails identically on a clean `development` checkout with no Scenario 19 changes applied (confirmed via `git stash`) — pre-existing, unrelated. Found 2026-08-04, not fixed.
- **2026-08-05 full re-audit, highest-severity finding**: Scenario 10's frontend AP UI (`VoucherPanel.tsx`, `SupplierDebitMemoDialog.tsx`, the `ap-payment-methods` page, PO document download) merged to `development` via PR #91, but the matching backend branch (`feat/scenario-10-purchasing-ap`, tip `5180eba`) was never merged — these are live, clickable features on `development` today that 404 on submit for any real user. Recommend fast-tracking that backend PR or feature-flagging the frontend surfaces until it lands. Scenario 13's supplier-debit-memo gap is the same root cause, not a separate bug. — **RESOLVED 2026-08-07**: the backend branch merged as PR #89 (`6fd7ffd`, 2026-08-05/06); re-verified every frontend call site against the real backend routes (path, HTTP method, DTO shape) for all 5 previously-broken surfaces plus match-check and the cheque document — all now match exactly, no remaining mismatches. Scenario 13's supplier-debit-memo 404 resolved the same way.
- Scenario 15's row previously said both PRs (#103 frontend, #99 backend) were "pending review/merge" — re-verified 2026-08-05 via `git merge-base --is-ancestor`, both are confirmed merged (`7a58bf9`, `044ab1b`); row flipped to closed. Separately, the plan doc's claim that `BranchPricing` is dead code is itself inaccurate — `branch-pricing.controller.ts`/`.service.ts` and a full frontend page (wired into the sidebar) exist and predate the plan doc; it's a parallel pricing mechanism, not dead.
- Scenario 19's parent checkbox was left unchecked despite the plan doc's own log showing full implementation (2026-08-04) — re-verified 2026-08-05, flipped to closed; only the explicitly-deferred serial-level item remains open. (Separately, and NOT part of this audit per instruction: a local, uncommitted `feat/scenario-19-...-v2` branch with further stock-count work — branch scoping — is currently mid-merge and untouched in both repos' working copies.)
- Scenario 20's plan doc still listed "ReminderController/InteractionController have no PermissionsGuard at all — any authenticated user can create/complete/delete any reminder or interaction" as an open risk. Re-verified 2026-08-05: fixed in commit `213cb81`, which also granted Branch Manager/Cashier the CRM permissions they'd need to keep working once that guard was enforced (they'd previously been granted none), and added collections context (customer/lead/installment-account label) to the generic reminders list — closing another "worth flagging" item from the same doc. Plan doc's own Implementation Log needs updating to reflect this; checklist row was already accurate since it only tracked the DAM chain item as open.
- Scenario 18's Quarantine + inspection step (Part 1) is fully built and ready on unmerged branch `feat/scenario-18-returns-exchanges-disposition` (`b91c9df`) — genuinely not on `development` yet, so "not started" remains correct, but it's not untouched work either.
- Scenario 11: found a real, previously-untracked gap — `recordPayment()` is still a single direct action with no OR-stub/remittance two-step workflow (the original plan doc's Closing Gap #4). Added as a new sub-item. **Re-checked 2026-08-07**: a new POS Collections screen merged this pass (branch/collector/payment-method capture on AR payments, same-day duplicate guard, collections customer list — PR #108/#115) — adjacent and related, but it's still a single-step `recordPayment()` under the hood; doesn't touch the OR-stub/remittance gap, which remains open.
- The RBAC self-escalation bugs tracked separately (`RolesController` has no `PermissionsGuard`; `POST /users` has no permission check) were re-confirmed still live and unpatched on `development` as of 2026-08-05 — now cross-referenced under Scenario 21, since any maker≠checker/approval-limit design there would inherit the same hole. **Clarified 2026-08-07**: `RolesController` already carries `@RequirePermissions(...)` decorators on every route — this is a pre-existing state, at least as old as 2026-07-19 per prior investigation, not something that changed this pass — but `PermissionsGuard` (the guard that actually reads those decorators) is never applied to the controller and isn't registered globally (`app.module.ts` has no `APP_GUARD` provider for it, `main.ts` has no `useGlobalGuards()`), so nothing enforces them: every roles endpoint is still callable by any authenticated user. `POST /users` is unchanged and still unguarded. Worth flagging to whoever is expected to fix this — the decorators read as done at a glance, aren't, and could get checked off by mistake.
- This pass's audit was run against clean, isolated `git worktree` checkouts of `origin/development` in both repos, not the local working copies — both local checkouts are currently mid-merge on the unrelated, unfinished `feat/scenario-19-stock-count-adjustment-approval-v2` branch (per developer instruction, left untouched and not inspected).
- Scenario 17 (2026-08-06): asking "is anything missing?" after a scenario is marked closed surfaced two real bugs the happy-path manual test steps never would have — `PromissoryNote.creditApplicationId` being `@unique` crashed any resubmit of an installment sale after its first hold was rejected/cancelled (the application stays approved/unconsumed, so the retry tried to insert a second PN row for it), and the only place to print/sign a Promissory Note was checkout's Pending Approval screen, plain React state lost on refresh/navigation with no way back. Both fixed and covered by new tests. Worth running this kind of adversarial pass (reject-then-retry, navigate-away-mid-flow) as standard before calling any approval-gated flow done, not just once a developer reports it.
- **2026-08-07 targeted re-audit** (prompted by a large volume of merges to `origin/development` in the 2 days since the last full pass — see updated "Last verified" line at the top): checked all rows against `origin/development` HEAD directly rather than the local working copies, since both had diverged from `development` again — backend is clean and on `development` but its HEAD had moved on; frontend is on an unrelated branch `fix/financing-term-edit-delete` with 6 uncommitted files (`ReceiveStockModal.tsx`, `ItemSearchCombobox.tsx`, `pos-actions.ts`, `usePos.ts`, `FinancingTermList.tsx`, `schema/pos/index.ts`) — left untouched, not inspected, not part of this audit.
- **2026-08-07**: found a new unmerged branch, `feat/inventory-transfer-partial-receipt` (backend) with a matching frontend branch, adding partial/reopenable stock transfer receiving (a transfer can be received in installments and stays open until fully received, plus a shared variance badge). This isn't a gap under any tracked scenario — Scenario 06 (Stock Request & Inter-Branch Transfer) is already closed and this is an enhancement on top of it, not a fix to a known gap — flagging only because it's live WIP that doesn't show up anywhere else in this doc.
- **Scenario 24, spot-checked 2026-08-10**: found via `git status`/`git diff` in both repos that the developer already has substantial closing-gap work in progress on `feat/scenario-24-collector-area-assignment` — all uncommitted, not staged, not yet a PR. Verified directly against the diffs (not just the plan doc's own claims): Closing Gaps 1-3 (address consolidation, structured `barangayCode` capture, `CollectorArea` coverage) are fully implemented end-to-end — schema migrations, service/DTO changes, frontend forms (`CollectorAreaPicker.tsx` is a new component), and both a Jest e2e block (`crm-collectors-installment-accounts.e2e-spec.ts`) and a Playwright spec (`crm-collectors-installment-accounts.spec.ts`) covering create/edit/replace/clear/cascade-delete/overlap. Closing Gap 4 (auto-assignment) only landed on the backend — the suggest-collector endpoint exists and is wired into account creation, but neither installment-account form calls it, so today's real user-facing behavior is unchanged (still a flat dropdown) except for the silent fallback when a collector is left unpicked entirely. Nothing here has been committed, so none of it is at risk of being mistaken for merged/shipped work — flagging so the checklist reflects reality if this session's context is lost before the developer commits. **SUPERSEDED same day**: Closing Gap 4's frontend gap noted here was closed within the same 2026-08-10 session — see the entry immediately below.
- **Scenario 24, closed 2026-08-10**: Closing Gap 4's frontend wiring (the gap the same-day spot-check above flagged) completed — `NewInstallmentAccountForm` now calls the suggest-collector preview endpoint once a customer is picked, pre-selects the match while still showing the full collector list, and labels it "— suggested" (developer's confirmed choice over a filtered-dropdown or fully-silent design). Also branch-scoped the suggestion logic to match `CollectorService`'s existing convention (not originally asked for, but without it a Branch Manager could be auto-assigned a collector outside their own scope) — covered by a new backend e2e test. Separately, live manual testing of Part 3 surfaced a real, unrelated-to-this-scenario UI gap in the Collectors list — no clickable row (only the stub-number cell linked out), no delete action anywhere in the Collector UI despite the backend route existing — both fixed and covered by 2 new Playwright tests. All 4 closing-gap items and all 3 open questions are now resolved; full detail in the plan doc's Implementation Log.
- **Scenario 24, same-day follow-ups 2026-08-10** (after the developer continued live testing post-closure): `EditInstallmentAccountForm` now gets the same collector suggestion `NewInstallmentAccountForm` has, but only auto-selects when the account has no collector yet — never overrides an existing assignment. A real bug (not just a display nitpick) was found this way: a test customer's address looked complete but had `barangayCode: null` because a real barangay ("PHHC Block 17," which happens to read like a street name) had been typed into the free-text street box instead of clicked from the Barangay dropdown — both produce an identical-looking composed address, so it was undetectable after the fact. Fixed at the source in `PhilippineAddressPicker` (a live "barangay selected / not selected" status line) and backfilled the one existing affected record. Also fixed: Installment Accounts list rows weren't fully clickable (same gap/fix as Collectors, above); and — per explicit developer request after comparing Create's and Edit's very different field sets — added a read-only "Account summary" card (customer + original financing terms) to the top of the Edit form, chosen over a full Create/Edit form-component merge as the smaller, lower-risk way to solve the same visibility problem.
- **Scenario 23, closed 2026-08-09**: all 5 closing-gap items implemented, tested (unit + e2e + Playwright), and manually confirmed by the developer on `feat/scenario-23-invoice-lookup-consistency`. Full detail in the plan doc's Implementation Log. Same pattern as Scenario 17 (2026-08-06 entry above) — a large amount of unplanned, real bugs surfaced from the developer's own live testing against a reseeded DB, not from this scenario's original scope: RBAC over/under-grants on the Cashier role and ~20 unnecessarily-guarded read-only routes; a "soft-deleted customer's record lingers on live list screens forever" bug class found and fixed across 6 unrelated services (`InstallmentAccountService`, `CreditApplicationService`, `ReminderService`, `SkuReservationsService`, `CustomerAdvancesService`, `ServiceDraftsService`); a `count()`-based sequential-numbering bug in `credit-application.service.ts` that breaks permanently after any row deletion (same class as the `PR-`/`ADJ-`/`CNT-`/PO-code generators already flagged elsewhere in this doc — worth checking whether `service-drafts.service.ts`'s `SDI-` generator has the same flaw, not yet checked); a release-form-request approval that incorrectly required the cashier's original session to still be open; a raw-UUID error message shown directly to cashiers at checkout; and an app-wide gap where `ARInvoiceStatus`/`APBillStatus`'s `OVERDUE` value was write-only (only set during payment recording) and never recomputed on a read, so a fully-unpaid past-due record displayed as "Due" forever across at least 10 different screens/reports. None of this was tracked before being found live this pass — worth a scan of whether any should be back-filled into Scenario 17's own doc.
- **2026-08-10 full re-audit** (all 25 scenarios, dispatched as 5 parallel batches of 5 each, cross-checked against `development` HEAD in both repos rather than trusting this doc's own checkmarks — the same drift-catching method used to first spot Scenario 08's staleness earlier the same day). 21 of 25 scenarios' entries were independently reconfirmed accurate as written, including a full re-run of the Scenario 18 unmerged-branch check (still genuinely unmerged in both repos, hashes match exactly). Four real corrections made: **Scenario 08** (Caravan) was still marked "2 small items open" but both — the Consign-to-Branch UI button and event name/date capture — shipped 2026-08-07 (#117 frontend `ae0802a`, #110 backend `3dd56a9`); flipped to fully closed. Separately confirmed the scenario's original 5-item core (consigned-stock state, the Caravan tab, split inventory/COGS attribution to origin with cash staying at host, the `caravanOriginBranchId` accounting tag, and the return/onward-at-close flow) had already shipped back on 2026-07-27 — the checklist's terse "2 small items open" summary was accurate but easy to misread as "barely started," when in fact only late-stage polish remained. **Scenario 10**'s voucher-attachments sub-item was stale — `CreateVoucherDto` itself never grew an attachment field as the doc had checked for, but attachments were built through the pre-existing generic `FileAttachments` entity-link subsystem instead (`VoucherPanel.tsx` + `file-attachments.controller/service.ts`, frontend `681705f` 2026-08-04); only the auto-1%-withholding gap remains open on that scenario. **Scenario 11**'s remittance sub-item was reworded — a `CollectorRemittance` cash-remittance log already existed (since 2026-07-28, predating the item's own "found 2026-08-05" note) but isn't linked to individual payments/OR numbers or used as a pending-confirm gate, so the deeper ledger-gated two-step workflow the plan doc describes is still genuinely missing. **Scenario 21**'s "blocking prerequisite risk" sub-item was the most consequential drift: it accurately described `RolesController`/`POST /users` as unguarded as of its 2026-08-07 recheck, but Scenario 22's own fix (commit `eee202c8`, 2026-08-08, merged 2026-08-10 via PR #112) closed that exact hole one day later — both are now `PermissionsGuard`-gated. Marked resolved, with a narrower residual risk noted (no check on _which_ roles an `admin:users:create` holder may grant). Scenario 13 also got a cosmetic date fix (supplier-side resolution was 2026-08-06, not 08-07).
- **Scenario 25, closed 2026-08-10**: implemented on `feat/scenario-25-ar-invoice-detail-view`, branched off `feat/scenario-24-collector-area-assignment` — landed after the full re-audit above, which had still found it "not started." Closing Gaps 1-2 done as one combined part (developer's call — tightly coupled, nothing to link to from CRM until the detail page exists): new `/accounting/ar-invoices/[id]` page mirrors Purchase Orders' exact `printInventoryDocument()` print pattern, `ARInvoicesService.findOne()` gained an `installmentDetail` field (product/brand/rebate, resolved per Open Question 2's confirmed scope) served under the existing `accounting:ar-invoices:read` permission (no second permission check needed — deliberately doesn't call the separately-gated `pos:collections:manage` endpoint), and Customer360's Installment Plan modal rows now link straight into it. Closing Gap 3 needed no code — confirmed via Phase 1 re-verification that Scenario 23's Closing Gap 5 already closed the loophole it depended on. Live-testing follow-up: AR Invoices list rows made fully clickable, matching the same convention already applied to Collectors/Installment Accounts/Customers lists this same day under Scenario 24.
- **Scenario 09, closed 2026-08-10**: Closing Gap 6 (type of service at job creation) implemented — new `ServiceCategory` enum + `ServiceDraftServiceType` child model, validated against NIG's real 6-category service catalog, a repeatable "Types of Service" section on the form (deliberately not pre-populated by default — a pre-added-but-empty row blocked submission on every pre-existing flow, found live and fixed), materials auto-suggestion for the electrical-part sub-types, and a running total on the detail view. Kept optional at the API layer rather than required, since hard-requiring it would have broken 38 existing test call sites against an already-shipped endpoint — a deliberate deviation from the plan doc's literal wording. Also closed a new developer-requested follow-up not in the original 6-item plan: `ServiceDraft.posTransactionId` had existed since Closing Gap 2 but had no UI; added a "Linked Sale" picker/display so a service job can be traced back to the POS transaction/invoice it was sold on. Found and fixed a real bug along the way — the linked-sale search crashed silently on `t.totalAmount.toFixed(2)` (Decimal serializes as a string over the API, not a number), the same class of bug already flagged elsewhere in this scenario's own doc for invoice-line amounts. Both parts e2e-tested (backend + Playwright) and manually confirmed by the developer. Full detail in the plan doc's Implementation Log.
- **2026-08-10 re-verification pass**, run after the full re-audit above went through a manual `git stash pop` merge conflict (a human hand-merged the header and this Notes section after pulling Scenario 25's own checklist edit) and then landed on `development` via PR #123 — manual conflict resolution is error-prone, so this pass specifically re-checked all 25 scenarios for two things: (1) textual coherence — no garbled sentences, duplicated paragraphs, or checkbox states contradicting their own prose, anywhere including this Notes section and the header's "last verified" stack; (2) continued code accuracy at the merged HEAD (frontend `907fcda`, backend `056bcb5`). Dispatched as 5 parallel batches, same method as the original full re-audit. Result: no merge-conflict artifacts found anywhere in the document — the hand-merge held up cleanly. 24 of 25 scenarios' entries confirmed accurate as written. One real, unrelated-to-the-merge drift found: **Scenario 19**'s "serial-level count reconciliation — explicitly deferred" sub-item was stale — it actually shipped the same day the scenario was marked closed (commit `b4c692b`, "...scenario 19 Part 5," 2026-08-05), backed by a dedicated migration, a 414-line e2e spec, and frontend serial-picker wiring; flipped to resolved. Two cosmetic-only nits also fixed: this doc's opening scope note never accounted for Scenarios 22-25 (added the missing coverage note), and Scenario 15's commission-conflict sub-item cited the wrong model (`AgentCommission.commissionRate` → corrected to `Agent.commissionRate`) — both pre-existing, neither a symptom of the merge.
